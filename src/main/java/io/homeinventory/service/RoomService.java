package io.homeinventory.service;

import org.springframework.stereotype.Service;

@Service
public class RoomService {
    // TODO: inject RoomRepository, ItemRepository
    // findByUserId(), save(), delete() — rejects if items exist
}
